# Base variables shared by services when running Docker Compose for development.
# Copy to `env/dev.env` and customise as needed.
#
# Containers always run with `NODE_ENV=production`; override `WEB_NODE_ENV` or
# `API_NODE_ENV` if you truly need a different runtime mode.

WEB_PORT=3000
API_PORT=5000

# URLs
NEXTAUTH_URL=http://localhost:3000
AUTH_BACKEND_URL=http://finance-management-api:5000
NEXT_PUBLIC_PASSWORD_RESET_REDIRECT_URL=http://localhost:3000/reset-password

# Redis
REDIS_URL=redis://finance-management-redis:6379
REDIS_CONNECT_TIMEOUT_MS=10000

# MongoDB (used together with the optional `local-db` profile)
MONGO_USERNAME=root
MONGO_PASSWORD=example
MONGO_DATABASE=finance-management
MONGO_URI=mongodb://root:example@finance-management-db:27017/finance-management?authSource=admin

# API
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Authentication secrets (replace in your real environment)
NEXTAUTH_SECRET=dev-insecure-secret
ACCESS_TOKEN_SECRET=dev-insecure-secret
REFRESH_TOKEN_SECRET=dev-insecure-secret
