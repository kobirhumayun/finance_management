# The Compose configuration has been reorganized under the compose/ directory.
# Use docker compose -f compose/base.yml -f compose/development.yml --profile stack up for development
# or swap in compose/production.yml for production deployments.

services: {}
