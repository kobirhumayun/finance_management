# Base variables shared by services when running Docker Compose in production-like environments.
# Copy to `env/prod.env` and replace the placeholder values.
#
# Containers always run with `NODE_ENV=production`; override `WEB_NODE_ENV` or
# `API_NODE_ENV` only when debugging.

WEB_PORT=3000
API_PORT=5000

# URLs
NEXTAUTH_URL=https://finance.example.com
AUTH_BACKEND_URL=https://api.finance.example.com
NEXT_PUBLIC_PASSWORD_RESET_REDIRECT_URL=https://finance.example.com/reset-password

# Redis
REDIS_URL=redis://finance-management-redis:6379
REDIS_CONNECT_TIMEOUT_MS=10000

# MongoDB (point to your managed database cluster)
MONGO_URI=mongodb+srv://username:password@cluster.example.com/finance-management

# API
CORS_ALLOWED_ORIGINS=https://finance.example.com

# Authentication secrets
NEXTAUTH_SECRET=replace-with-strong-random-value
ACCESS_TOKEN_SECRET=replace-with-strong-random-value
REFRESH_TOKEN_SECRET=replace-with-strong-random-value
